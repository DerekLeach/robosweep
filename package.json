{
  "name": "robosweep",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "tsc --build src/scripts",
    "watch": "tsc --watch --project src/scripts/tsconfig.json"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.16",
    "typescript": "^5.0.4"
  }
}